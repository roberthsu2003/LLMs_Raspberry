# 權限操作練習 – 學生實作手冊

歡迎來到權限操作實作練習！權限管理是 Linux 系統安全的核心，掌握權限操作技能是成為 Linux 使用者的重要一步。請在您的 Raspberry Pi（或任何 Linux 終端）上逐一完成以下任務。

---

## 📋 練習前準備

在開始之前，請先建立練習環境：

```sh
# 建立練習目錄
mkdir -p ~/permission_practice
cd ~/permission_practice

# 確認目前位置
pwd
```

---

## 1️⃣ 檢視與理解預設權限

### 任務目標
學會查看檔案的權限資訊，理解權限欄位的意義。

### 操作步驟
1. 建立一個測試檔 `demo.txt`：
   ```sh
   touch demo.txt
   ```

2. 用 `ls -l demo.txt` 觀察權限欄位：
   ```sh
   ls -l demo.txt
   ```

3. **記錄與觀察：**
   - 記錄下顯示的 10 個字元（例：`-rw-r--r--`）
   - 第一個字元代表什麼？（`-` 表示檔案，`d` 表示目錄）
   - 後 9 個字元分為三組，分別代表什麼？

4. 用 `stat demo.txt` 取得更詳細資訊：
   ```sh
   stat demo.txt
   ```

5. **記錄與思考：**
   - 寫下你的使用者名稱（Uid）與群組名稱（Gid）
   - 檔案的預設權限是什麼？

### 預期結果
- 能夠讀懂 `ls -l` 輸出的權限欄位
- 理解所有者、群組、其他人的權限概念
- 知道如何查看更詳細的權限資訊

### 自我檢查
- [ ] 我能解釋 `-rw-r--r--` 的每個字元代表什麼
- [ ] 我了解檔案和目錄在權限顯示上的差異
- [ ] 我知道如何查看檔案的擁有者和群組

---

## 2️⃣ 符號式變更權限

### 任務目標
學會使用符號式（`u`, `g`, `o`, `a`）來變更檔案權限。

### 操作步驟
1. 為 `demo.txt` **加入執行權限**（所有者）：
   ```sh
   chmod u+x demo.txt
   ls -l demo.txt
   ```
   - **觀察：** 權限欄位發生了什麼變化？

2. **移除群組的寫入權限**：
   ```sh
   chmod g-w demo.txt
   ls -l demo.txt
   ```
   - **觀察：** 群組的權限如何變化？

3. **給其他人加入執行權限**：
   ```sh
   chmod o+x demo.txt
   ls -l demo.txt
   ```
   - **確認：** `ls -l` 中的最後一位是否為 `x`？

4. 使用 **一次變更多組**：
   ```sh
   chmod a+r demo.txt
   ls -l demo.txt
   ```
   - **觀察：** 所有三組的讀取權限都變成了 `r`

### 預期結果
- 能夠使用 `u`, `g`, `o`, `a` 來指定要修改的對象
- 理解 `+`, `-`, `=` 的作用
- 能夠組合多個權限變更

### 自我檢查
- [ ] 我了解 `u`, `g`, `o`, `a` 分別代表什麼
- [ ] 我知道如何使用 `+`, `-`, `=` 來變更權限
- [ ] 我能解釋每次 `chmod` 後權限的變化

---

## 3️⃣ 使用八進位模式設定常見權限

### 任務目標
學會使用數字模式（八進位）快速設定權限。

### 操作步驟
1. 把 `demo.txt` 的權限改為 **644**：
   ```sh
   chmod 644 demo.txt
   ls -l demo.txt
   ```
   - **說明：** 每個數字代表什麼權限？
   - **思考：** 644 是常見的檔案權限，為什麼？

2. 把 `demo.txt` 改為 **755**：
   ```sh
   chmod 755 demo.txt
   ls -l demo.txt
   ```
   - **觀察：** `ls -l` 中的變化
   - **思考：** 755 通常用於什麼類型的檔案？

3. 為 `demo.txt` 設定 **600**：
   ```sh
   chmod 600 demo.txt
   ls -l demo.txt
   ```
   - **確認：** 只有所有者能讀寫，其他人完全沒有權限

4. **對照練習：** 嘗試將權限改回 644，然後用符號式達成相同效果：
   ```sh
   chmod 644 demo.txt
   # 現在用符號式達成相同效果
   chmod u=rw,g=r,o=r demo.txt
   ls -l demo.txt
   ```

### 預期結果
- 能夠使用數字模式快速設定權限
- 理解常見權限數字（644, 755, 600, 777）的意義
- 能夠在符號式和數字模式間轉換

### 自我檢查
- [ ] 我能解釋 644、755、600 每個數字的意義
- [ ] 我知道什麼時候應該使用數字模式
- [ ] 我能在符號式和數字模式間轉換

---

## 4️⃣ 目錄的執行權限實作

### 任務目標
理解目錄的執行權限（`x`）與檔案執行權限的差異。

### 操作步驟
1. 建立目錄 `secret_folder`：
   ```sh
   mkdir secret_folder
   ```

2. 先只給所有者 **完全權限**：
   ```sh
   chmod 700 secret_folder
   ls -ld secret_folder
   ```
   - **觀察：** 權限顯示為 `drwx------`

3. 嘗試進入目錄：
   ```sh
   cd secret_folder
   pwd
   cd ..
   ```
   - **確認：** 可以進入（因為有 `x` 權限）

4. 把 **群組的執行權限** 加回：
   ```sh
   chmod 750 secret_folder
   ls -ld secret_folder
   ```
   - **觀察：** 權限顯示為 `drwxr-x---`

5. **進階實驗：** 移除執行權限，觀察結果：
   ```sh
   chmod 740 secret_folder  # 移除群組的執行權限
   cd secret_folder  # 應該會失敗（如果群組成員嘗試）
   chmod 750 secret_folder  # 恢復
   ```

### 預期結果
- 理解目錄的 `x` 權限代表「可進入」
- 知道目錄權限與檔案權限的差異
- 能夠設定適當的目錄權限

### 自我檢查
- [ ] 我了解目錄的 `x` 權限和檔案的 `x` 權限有什麼不同
- [ ] 我知道如何設定目錄權限讓特定使用者可以進入
- [ ] 我理解為什麼目錄需要 `x` 權限才能 `cd` 進去

---

## 5️⃣ 改變擁有者與群組（需 sudo）

### 任務目標
學會使用 `chown` 和 `chgrp` 改變檔案的擁有者和群組。

> **提示**：若你沒有 sudo 權限，請先向老師申請或在本機模擬使用 `sudo`。

### 操作步驟
1. 用 `chown` 把檔案的擁有者與群組改為自己：
   ```sh
   sudo chown $(whoami):$(whoami) demo.txt
   ls -l demo.txt
   ```
   - **觀察：** 擁有者和群組欄位的變化

2. 用 `chgrp` 改變群組：
   ```sh
   # 先查看系統有哪些群組
   groups
   
   # 嘗試改變群組（如果系統有 staff 群組）
   sudo chgrp staff demo.txt 2>/dev/null || sudo chgrp $(whoami) demo.txt
   ls -l demo.txt
   ```
   - **觀察：** 群組欄位的變化

3. **進階實驗：** 改變擁有者為 root（如果可能）：
   ```sh
   sudo chown root:root demo.txt
   ls -l demo.txt
   # 觀察擁有者變成了 root
   
   # 改回自己的擁有者
   sudo chown $(whoami):$(whoami) demo.txt
   ```

### 預期結果
- 能夠使用 `chown` 改變檔案擁有者
- 能夠使用 `chgrp` 改變檔案群組
- 理解為什麼需要 `sudo` 權限

### 自我檢查
- [ ] 我了解 `chown` 和 `chgrp` 的差異
- [ ] 我知道為什麼改變擁有者需要 `sudo`
- [ ] 我能正確使用 `chown user:group file` 的語法

---

## 6️⃣ 結合權限與執行腳本

### 任務目標
理解檔案執行權限與腳本執行的關係。

### 操作步驟
1. 建立簡易腳本 `runme.sh`：
   ```sh
   cat > runme.sh << 'EOF'
   #!/bin/bash
   echo "執行成功！"
   EOF
   ```

2. 先查看目前的權限：
   ```sh
   ls -l runme.sh
   ```

3. **只給自己執行權限**：
   ```sh
   chmod 700 runme.sh
   ls -l runme.sh
   ```

4. 嘗試執行：
   ```sh
   ./runme.sh
   ```
   - **觀察：** 是否成功執行？

5. 改成 **所有人可執行**：
   ```sh
   chmod 755 runme.sh
   ls -l runme.sh
   ./runme.sh
   ```
   - **說明：** 兩次執行的差異是什麼？

6. 把腳本的 **寫入權限移除**：
   ```sh
   chmod a-w runme.sh
   ls -l runme.sh
   ```

7. 嘗試編輯（應該會被阻止）：
   ```sh
   nano runme.sh
   # 嘗試修改並儲存，觀察系統的反應
   ```

8. 恢復寫入權限：
   ```sh
   chmod u+w runme.sh
   ```

### 預期結果
- 理解執行權限對腳本的重要性
- 知道如何保護腳本不被修改
- 能夠設定適當的腳本權限

### 自我檢查
- [ ] 我了解為什麼腳本需要 `x` 權限才能執行
- [ ] 我知道如何保護腳本不被修改
- [ ] 我能設定適當的腳本權限（如 755 或 700）

---

## 7️⃣ 關鍵觀念實作：目錄權限 vs 檔案權限

### 任務目標
透過實作深刻理解「檔案的刪除權限取決於目錄權限」這個重要觀念。

### 操作步驟
1. 建立測試環境：
   ```sh
   mkdir test_dir
   touch test_dir/test_file.txt
   chmod 777 test_file.txt  # 給檔案最高權限
   ls -l test_dir/test_file.txt
   ```

2. 查看目錄權限：
   ```sh
   ls -ld test_dir
   ```

3. **實驗 1：目錄有寫入權限**
   ```sh
   chmod 755 test_dir  # 目錄有寫入權限（所有者）
   ls -ld test_dir
   rm test_dir/test_file.txt  # 應該可以刪除
   ```

4. **重新建立檔案並實驗 2：目錄沒有寫入權限**
   ```sh
   touch test_dir/test_file.txt
   chmod 555 test_dir  # 移除目錄的寫入權限
   ls -ld test_dir
   rm test_dir/test_file.txt  # 應該會失敗！
   ```

5. **觀察錯誤訊息：**
   - 記錄下錯誤訊息
   - **思考：** 為什麼檔案有 777 權限，卻無法刪除？

6. **恢復目錄權限：**
   ```sh
   chmod 755 test_dir
   rm test_dir/test_file.txt
   rmdir test_dir
   ```

### 預期結果
- 深刻理解「檔案的刪除權限取決於目錄權限」
- 知道如何設定目錄權限來控制檔案的刪除
- 能夠解釋為什麼檔案權限無法控制刪除操作

### 自我檢查
- [ ] 我理解為什麼檔案有 777 權限卻無法刪除
- [ ] 我知道檔案的刪除權限取決於什麼
- [ ] 我能解釋目錄的 `w` 權限對檔案操作的影響

---

## 8️⃣ 進階挑戰：建立安全的專案結構

### 任務目標
綜合運用所學，建立一個具有適當權限的專案結構。

### 操作步驟
1. 在家目錄下建立 `project`，裡面依序建立 `src`、`docs`、`tests` 三個子目錄：
   ```sh
   mkdir -p ~/project/{src,docs,tests}
   ```

2. 在 `src` 中建立 `app.py`：
   ```sh
   cat > ~/project/src/app.py << 'EOF'
   #!/usr/bin/env python3
   print("Hello, Linux!")
   EOF
   ```

3. 設定 `app.py` 為 **所有人可讀、所有者可執行**：
   ```sh
   chmod 744 ~/project/src/app.py
   ls -l ~/project/src/app.py
   ```

4. 執行腳本：
   ```sh
   ~/project/src/app.py
   # 或
   python3 ~/project/src/app.py
   ```

5. 把 `src` 目錄的權限改為 **770**：
   ```sh
   chmod 770 ~/project/src
   ls -ld ~/project/src
   ```

6. **設定其他目錄的權限：**
   ```sh
   chmod 755 ~/project/docs  # 文件目錄：所有人可讀
   chmod 750 ~/project/tests  # 測試目錄：群組可讀
   ```

7. 檢視整個專案結構：
   ```sh
   tree ~/project
   ls -lR ~/project
   ```

### 預期結果
- 能夠為不同類型的目錄設定適當的權限
- 理解專案中不同目錄的權限需求
- 能夠建立安全的專案結構

### 自我檢查
- [ ] 我能為不同用途的目錄設定適當權限
- [ ] 我理解為什麼不同目錄需要不同的權限
- [ ] 我能建立一個安全的專案權限結構

---

## 📝 作業提交要求

完成所有練習後，請整理一份學習報告，包含以下內容：

### 1. 操作記錄
- 每個任務的主要指令和輸出結果
- 可以使用終端機的「複製」功能或截圖
- 特別記錄第 7 題的實驗結果

### 2. 問題與解決
- 遇到的任何錯誤訊息
- 你是如何解決這些問題的
- 特別是在第 5 題的 sudo 操作中遇到的問題

### 3. 學習心得
- 哪個部分最困難？為什麼？
- 「目錄權限 vs 檔案權限」這個觀念對你有什麼啟發？
- 你學到了什麼新的權限管理技巧？

### 4. 挑戰任務成果
- 進階挑戰的完成結果
- 專案結構的權限設定
- 你對專案權限設計的思考

---

## 💡 學習提示

1. **安全第一**：在修改系統檔案的權限前，務必先確認
2. **理解概念**：權限的數字和符號式都要理解，不要死記
3. **多做實驗**：嘗試不同的權限組合，觀察結果
4. **記錄筆記**：記錄常見的權限設定（644, 755, 600 等）
5. **關鍵觀念**：記住「檔案的刪除權限取決於目錄權限」

---

## 🎯 學習目標檢核

完成所有練習後，您應該能夠：

- [ ] 使用 `ls -l` 讀懂權限欄位
- [ ] 使用符號式（`chmod u+x`）變更權限
- [ ] 使用數字模式（`chmod 755`）設定權限
- [ ] 理解目錄的執行權限與檔案的執行權限的差異
- [ ] 使用 `chown` 和 `chgrp` 改變擁有者和群組
- [ ] 理解「檔案的刪除權限取決於目錄權限」這個關鍵觀念
- [ ] 為不同類型的檔案和目錄設定適當的權限
- [ ] 建立安全的專案權限結構

祝您練習順利！期待看到您的學習成果！
