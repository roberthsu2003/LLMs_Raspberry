# Open WebUI 工具 (Tools) 完整介紹

## 📖 什麼是 Tools？

**Tools** 是擴展 LLM 能力的 Python 腳本，讓 LLM 能夠執行超出簡單文字生成的功能。當啟用後，它們允許你的聊天機器人執行各種任務，例如：
- 🔍 搜尋網路
- 📊 抓取資料
- 🎨 生成圖片
- 🗣️ 使用 AI 語音回覆
- 以及更多功能

Tools 本質上是讓 LLM 能夠與外部世界互動的能力，提供**即時、真實世界的資料**，這些是模型僅憑預訓練知識無法獲得的。

---

## 🧩 工具分類：你使用的是哪種「Tool」？

在 Open WebUI 中，你可能會在不同情境下遇到「Tools」這個術語。以下是區分它們的方式：

| 類型 | UI 位置 | 最佳用途 | 來源 |
|------|---------|----------|------|
| **原生功能** | Admin/Settings | 核心平台功能 | 內建於 Open WebUI |
| **Workspace Tools** | `Workspace > Tools` | 使用者創建或社群 Python 腳本 | [社群工具庫](https://openwebui.com/search) |
| **原生 MCP (HTTP)** | `Settings > Connections` | 可透過 HTTP/SSE 存取的標準 MCP 伺服器 | 外部 MCP 伺服器 |
| **MCP via Proxy (MCPO)** | `Settings > Connections` | 本地 stdio 型 MCP 伺服器（如 Claude Desktop 工具） | [MCPO 適配器](https://github.com/open-webui/mcpo) |
| **OpenAPI 伺服器** | `Settings > Connections` | 標準 REST/OpenAPI 網路服務 | 外部 Web API |

### 1. 原生功能 (Built-in)

這些功能深度整合到 Open WebUI 中，通常不需要外部腳本：

- **Web Search**: 透過 SearXNG、Google 或 Tavily 等引擎整合
- **URL Fetching**: 使用 `#` 或原生工具直接從網站提取文字內容
- **Image Generation**: 與 DALL-E、ComfyUI 或 Automatic1111 整合
- **Memory**: 讓模型能夠在對話中記住關於你的事實
- **RAG (Knowledge)**: 查詢上傳文件的能力（使用 `#`）

在**[原生模式](#工具調用模式-default-vs-native)**下，這些功能會作為**Tools**暴露，模型可以獨立調用。

### 2. Workspace Tools (自訂插件)

這些是**Python 腳本**，直接在 Open WebUI 環境中執行：

- **能力**: 可以做任何 Python 能做的事（網頁抓取、複雜數學、API 呼叫）
- **存取**: 透過 `Workspace` 選單管理
- **安全性**: 導入前務必審查程式碼，因為這些腳本會在伺服器上執行

⚠️ **安全警告**: 不應授予一般或不信任的使用者存取 Workspace Tools 區段的權限。此存取權限允許使用者在伺服器上上傳並執行任意 Python 程式碼，可能導致系統完全被入侵。

### 3. MCP (Model Context Protocol)

MCP 是一個開放標準，允許 LLM 與外部資料和工具互動：

- **原生 HTTP MCP**: Open WebUI 可以直接連接到任何暴露 HTTP/SSE 端點的 MCP 伺服器
- **MCPO (Proxy)**: 大多數社群 MCP 伺服器使用 `stdio`（本地命令列）。要在 Open WebUI 中使用這些，你需要使用 [**MCPO Proxy**](https://docs.openwebui.com/features/plugin/tools/openapi-servers/mcp) 來橋接連接

### 4. OpenAPI / Function Calling 伺服器

提供 OpenAPI (`.json` 或 `.yaml`) 規格的通用網路伺服器。Open WebUI 可以接收這些規格，並將每個端點視為一個工具。

---

## 📦 如何安裝和管理 Workspace Tools

Workspace Tools 是擴展實例最常見的方式，使用社群功能：

1. 前往 [社群工具庫](https://openwebui.com/search)
2. 選擇一個工具，然後點擊 **Get** 按鈕
3. 輸入你的 Open WebUI 實例 URL（例如 `http://localhost:3000`）
4. 點擊 **Import to WebUI**

### 安全提示

永遠不要導入你不認識或不信任的工具。這些是 Python 腳本，可能會在宿主系統上執行不安全的程式碼。**關鍵是，確保你只授予受信任的使用者「Tool」權限**，因為創建或導入工具的能力等同於在伺服器上執行任意程式碼的能力。

---

## 🔧 如何在聊天中使用 Tools

安裝或連接後，以下是如何在對話中啟用它們：

### 選項 1：即時啟用（特定聊天）

在聊天時，點擊輸入區域的 **➕ (加號)** 圖示。你會看到可用工具的列表——可以為該會話專門啟用它們。

### 選項 2：預設啟用（全域/模型層級）

1. 前往 **Workspace ➡️ Models**
2. 選擇你正在使用的模型，點擊 ✏️ 編輯圖示
3. 滾動到 **Tools** 區段
4. ✅ 勾選你希望此模型預設始終可存取的 Tools
5. 點擊 **Save**

你也可以讓你的 LLM 使用 [**AutoTool Filter**](https://openwebui.com/f/hub/autotool_filter/) 自動選擇正確的工具。

---

## 🛠️ 工具調用模式：Default vs. Native

Open WebUI 提供兩種不同的方式讓模型與工具互動：標準的**Default Mode**和高性能的**Native Mode (Agentic Mode)**。選擇正確的模式取決於你的模型能力和效能需求。

### 🟡 Default Mode (提示詞基礎)

在 Default Mode 中，Open WebUI 透過注入特定的提示詞模板來管理工具選擇，引導模型輸出工具請求。

- **相容性**: 適用於**幾乎所有模型**，包括缺乏原生函數調用支援的舊版或小型本地模型
- **靈活性**: 可透過提示詞模板高度自訂
- **注意事項**: 可能較慢（需要額外的 tokens），對於複雜的多步驟工具鏈接較不可靠

### 🟢 Native Mode (Agentic Mode / 系統函數調用)

Native Mode（也稱為 **Agentic Mode**）利用模型的內建能力來處理工具定義並返回結構化的工具調用（JSON）。這是**推薦的模式**，適用於高性能的代理工作流程。

#### 為什麼使用 Native Mode (Agentic Mode)？

- **速度與效率**: 延遲更低，因為避免了龐大的提示詞基礎工具選擇
- **可靠性**: 在遵循工具架構方面準確度更高（使用高品質模型時）
- **多步驟鏈接**: 對於**代理研究**和**交錯思考**至關重要，模型需要連續調用多個工具
- **自主決策**: 模型可以決定何時搜尋、使用哪些工具，以及如何組合結果

#### 如何啟用 Native Mode (Agentic Mode)

Native Mode 可以在兩個層級啟用：

1. **全域/管理員層級（推薦）**:
   - 導航至 **Admin Panel > Settings > Models**
   - 滾動到你目標模型的 **Model Specific Settings**
   - 在 **Advanced Parameters** 下，找到 **Function Calling** 下拉選單並選擇 `Native`

2. **每個聊天基礎**:
   - 在聊天中，點擊 ⚙️ **Chat Controls** 圖示
   - 前往 **Advanced Params** 並將 **Function Calling** 設為 `Native`

#### 模型需求與注意事項

**推薦用於 Agentic Mode 的模型**

為了可靠的代理工具調用，請使用高階前沿模型：
- **GPT-5** (OpenAI)
- **Claude 4.5 Sonnet** (Anthropic)
- **Gemini 3 Flash** (Google)
- **MiniMax M2.1**

這些模型在多步驟推理、正確的 JSON 格式化和自主工具選擇方面表現優異。

- **大型本地模型**: 一些大型本地模型（例如 Qwen 3 32B、Llama 3.3 70B）可以與 Native Mode 一起使用，但結果因模型品質而異
- **小型本地模型警告**: **小型本地模型**（參數少於 30B）通常難以使用 Native Mode。它們可能產生格式錯誤的 JSON、無法遵循嚴格的狀態管理，或做出糟糕的工具選擇決策。對於這些模型，**Default Mode** 通常更可靠

| 功能 | Default Mode | Native Mode |
|------|--------------|-------------|
| **延遲** | 中等/高 | 低 |
| **模型相容性** | 通用 | 需要工具調用支援 |
| **邏輯** | 提示詞基礎（Open WebUI） | 模型原生（API/Ollama） |
| **複雜鏈接** | ⚠️ 有限 | ✅ 優秀 |

---

## 🛠️ 內建系統工具 (Native/Agentic Mode)

當啟用 **Native Mode (Agentic Mode)** 時，Open WebUI 會自動注入強大的系統工具。這解鎖了真正的代理行為，讓能力強的模型（如 GPT-5、Claude 4.5 Sonnet、Gemini 3 Flash 或 MiniMax M2.1）能夠執行多步驟研究、探索知識庫或自主管理使用者記憶。

### 工具列表

#### 搜尋與網路

| 工具 | 用途 | 需求 |
|------|------|------|
| `search_web` | 搜尋公共網路以獲取資訊。最適合當前事件、外部參考或內部文件未涵蓋的主題 | `ENABLE_WEB_SEARCH` 已啟用 |
| `fetch_url` | 訪問 URL 並透過 Web Loader 提取文字內容 | Web Search 功能的一部分 |

#### 知識庫

| 工具 | 用途 | 需求 |
|------|------|------|
| `list_knowledge_bases` | 列出使用者可存取的知識庫及其文件數量 | 始終可用 |
| `query_knowledge_bases` | 透過語義相似性搜尋知識庫。找到名稱/描述與查詢意義匹配的知識庫。在查詢其文件之前使用此功能來發現相關知識庫 | 始終可用 |
| `search_knowledge_bases` | 按名稱和描述搜尋知識庫 | 始終可用 |
| `query_knowledge_files` | 使用簡單向量搜尋搜尋知識庫文件。**注意：**不使用混合搜尋或重新排序—對於完整的 RAG 管道與重新排序，請使用 File Context，透過 `#` 附加文件或分配知識庫 | 始終可用 |
| `search_knowledge_files` | 按檔名搜尋可存取知識庫中的文件 | 始終可用 |
| `view_knowledge_file` | 從知識庫獲取文件的完整內容 | 始終可用 |

#### 圖片生成

| 工具 | 用途 | 需求 |
|------|------|------|
| `generate_image` | 根據提示詞生成新圖片 | `ENABLE_IMAGE_GENERATION` 已啟用 |
| `edit_image` | 根據提示詞和圖片 URL 編輯現有圖片 | `ENABLE_IMAGE_EDIT` 已啟用 |

#### 記憶

| 工具 | 用途 | 需求 |
|------|------|------|
| `search_memories` | 搜尋使用者的個人記憶/個人化庫 | Memory 功能已啟用 |
| `add_memory` | 在使用者的個人化記憶中儲存新事實 | Memory 功能已啟用 |
| `replace_memory_content` | 透過唯一 ID 更新現有記憶記錄 | Memory 功能已啟用 |

#### 筆記

| 工具 | 用途 | 需求 |
|------|------|------|
| `search_notes` | 按標題和內容搜尋使用者的筆記 | `ENABLE_NOTES` 已啟用 |
| `view_note` | 獲取特定筆記的完整 markdown 內容 | `ENABLE_NOTES` 已啟用 |
| `write_note` | 為使用者創建新的私人筆記 | `ENABLE_NOTES` 已啟用 |
| `replace_note_content` | 更新現有筆記的內容或標題 | `ENABLE_NOTES` 已啟用 |

#### 聊天歷史

| 工具 | 用途 | 需求 |
|------|------|------|
| `search_chats` | 在聊天標題和訊息內容中進行簡單文字搜尋。返回匹配的聊天 ID 和片段 | 始終可用 |
| `view_chat` | 透過 ID 讀取並返回特定聊天的完整訊息歷史 | 始終可用 |

#### 頻道

| 工具 | 用途 | 需求 |
|------|------|------|
| `search_channels` | 按名稱/描述搜尋公開或可存取的頻道 | `ENABLE_CHANNELS` 已啟用 |
| `search_channel_messages` | 在可存取的頻道中搜尋特定訊息 | `ENABLE_CHANNELS` 已啟用 |
| `view_channel_message` | 查看頻道中的特定訊息或其詳細資訊 | `ENABLE_CHANNELS` 已啟用 |
| `view_channel_thread` | 查看頻道中的完整訊息線程/回覆 | `ENABLE_CHANNELS` 已啟用 |

#### 時間工具

| 工具 | 用途 | 需求 |
|------|------|------|
| `get_current_timestamp` | 獲取當前 UTC Unix 時間戳和 ISO 日期 | 始終可用 |
| `calculate_timestamp` | 計算相對時間戳（例如「3 天前」） | 始終可用 |

### 工具參數參考

詳細的工具參數請參考 [官方文件](https://docs.openwebui.com/features/plugin/tools/)。

### 知識庫工具 vs RAG 管道

原生 `query_knowledge_files` 工具使用**簡單向量搜尋**，預設返回 5 個結果。它**不使用**：
- 混合搜尋（BM25 + 向量）
- 重新排序（外部重新排序端點）
- 「Top K Reranker」管理員設定

對於完整的 RAG 管道（包含混合搜尋和重新排序），請使用 **File Context** 功能（透過 `#` 附加文件或知識庫分配），而不是依賴自主工具調用。

### 停用內建工具（每個模型）

**內建工具**功能可以在 **Workspace > Models** 編輯器中的 **Capabilities** 下為每個模型開啟或關閉。啟用時（預設），使用 Native Mode 時會自動注入所有上述系統工具。

**何時停用內建工具：**

| 情境 | 停用原因 |
|------|----------|
| 模型不支援函數調用 | 較小或較舊的模型可能無法正確處理 `tools` 參數 |
| 需要更簡單/可預測的行為 | 你希望模型僅使用預先注入的上下文，不進行自主工具調用 |
| 安全/控制考量 | 防止模型主動查詢知識庫、搜尋聊天、存取記憶等 |
| Token 效率 | 工具規格消耗 tokens；停用可節省上下文視窗空間 |

---

## 🧠 交錯思考 (Interleaved Thinking)

當使用 **Native Mode (Agentic Mode)** 時，高階模型可以進行**交錯思考**。這是一個強大的「思考 → 行動 → 思考 → 行動 → 思考 → ...」循環，模型可以推理任務、執行一個或多個工具、評估結果，然後決定下一步行動。

### 品質模型需求

交錯思考需要具有強大推理能力的模型。此功能最適合前沿模型（GPT-5、Claude 4.5+、Gemini 3+），這些模型可以在多個工具調用之間維持上下文，並智能地決定何時使用哪些工具。

這與單次工具調用根本不同。在交錯工作流程中，模型遵循以下循環：

1. **推理**: 分析使用者的意圖並識別資訊缺口
2. **行動**: 調用工具（例如，`query_knowledge_files` 用於內部文件，或 `search_web` 和 `fetch_url` 用於網路研究）
3. **思考**: 讀取工具的輸出並更新其內部理解
4. **迭代**: 如果答案不清楚，調用另一個工具（例如，`view_knowledge_file` 讀取特定文件或 `fetch_url` 讀取特定頁面）或改進搜尋
5. **完成**: 只有在完成這個「深度研究」循環後，模型才提供最終、有根據的答案

這種行為將標準聊天機器人轉變為能夠自主解決複雜、多步驟問題的**代理 AI**。

---

## ⚙️ Valves 和 UserValves

**Valves** 和 **UserValves** 是 Tools、Pipelines 和 Filters 的可選（但強烈建議）配置機制。

### Valves vs UserValves

- **Valves**: 僅由管理員透過 Tools 或 Functions 選單配置
- **UserValves**: 任何使用者都可以直接從聊天會話配置

兩者都在 GUI 中創建可填寫的欄位或布林開關，用於動態配置。

### 配置結構

Valves 和 UserValves 使用 Pydantic 的 `BaseModel` 定義為子類，必須在 Tools、Filter 或 Pipe 類中聲明為屬性。

**範例結構：**

```python
from pydantic import BaseModel, Field
from typing import Dict, Any

class Tools:
    class Valves(BaseModel):
        api_key: str = Field("", description="你的 API 金鑰")
        enable_feature: bool = Field(False, description="啟用功能")
    
    class UserValves(BaseModel):
        user_preference: str = Field("default", description="使用者偏好")
    
    def __init__(self):
        self.valves = self.Valves()
        self.user_valves = self.UserValves()
    
    def your_method(self, param: str) -> Dict[str, Any]:
        """
        方法描述
        :param param: 參數描述
        :return: 返回值描述
        """
        try:
            # 使用 self.valves.api_key 存取 Valves
            # 使用 __user__["valves"]["user_preference"] 存取 UserValves
            return {"status": "success", "data": result}
        except Exception as e:
            return {"status": "error", "message": str(e)}
```

### 在工具中存取

- **Valves**: 在程式碼執行時可直接存取，因為它們由管理員設定
- **UserValves**: 透過 `__user__` 參數可用（透過 `__user__["valves"]` 存取）

---

## 🐍 開發自己的工具

### 基本結構

Open WebUI 工具是 Python 腳本，以下是推薦的結構：

```python
"""
title: 你的工具名稱
author: 你的名字
version: 1.0
license: MIT
description: 你的工具做什麼
requirements: package1, package2
"""

from pydantic import BaseModel, Field
from typing import Dict, Any

class Tools:
    class Valves(BaseModel):
        API_KEY: str = Field(default="key", description="API 金鑰")
    
    def __init__(self):
        self.config = self.Valves()
    
    def your_method(self, param: str) -> Dict[str, Any]:
        """
        方法描述
        :param param: 參數描述
        :return: 返回值描述
        """
        try:
            # 你的實作
            return {"status": "success", "data": result}
        except Exception as e:
            return {"status": "error", "message": str(e)}
```

### 關鍵元素

- **元資料**: 在頂部包含 title、author、version、license、description 和 requirements
- **配置**: 使用 Pydantic `BaseModel` 進行配置類，帶有類型提示
- **類別名稱**: 必須使用 `class Tools` 讓 Open WebUI 識別
- **方法**: 包含文件字串、類型提示，以及帶有 try-except 區塊的錯誤處理
- **非同步支援**: 對於網路請求或資料庫查詢，使用 `async`/`await`

### 安裝

工具透過 `Workspace > Tools` 管理，可以從社群工具庫取得。GitHub 上有一個[模板骨架](https://github.com/pahautelman/open-webui-tool-skeleton)可供參考和最佳實踐。

---

## 🚀 總結與下一步

Tools 透過讓 AI 與世界互動來賦予 AI 生命。

- **瀏覽工具**: [openwebui.com/search](https://openwebui.com/search)
- **進階設定**: 了解更多關於 [MCP 支援](https://docs.openwebui.com/features/plugin/tools/openapi-servers/mcp)
- **開發**: [編寫你自己的自訂工具包](https://docs.openwebui.com/features/plugin/tools/development)

---

## 📚 參考資源

- [Open WebUI 官方文件 - Tools](https://docs.openwebui.com/features/plugin/tools/)
- [Open WebUI 官方文件 - Tools & Functions](https://docs.openwebui.com/features/plugin/)
- [社群工具庫](https://openwebui.com/search)
- [工具開發指南](https://docs.openwebui.com/features/plugin/tools/development)
- [MCP 支援文件](https://docs.openwebui.com/features/plugin/tools/openapi-servers/mcp)

---

**最後更新**: 2025年1月
