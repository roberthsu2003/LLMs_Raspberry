# file_server安裝流程說明(Samba)

用**「實務＋教學友善」**的方式，帶你把 **Raspberry Pi 變成檔案伺服器（File Server）**。

**最推薦方案（Samba）**，再補充其他選項，方便你依教學或實務選擇。

---

## **一、最推薦方案：使用 Samba（像 Windows 分享資料夾）**

👉 **適合情境**

- Windows / macOS / Linux 都要存取
- 教學環境、學校、家庭網路
- 最直覺、最好用

---

## **二、基本架構說明（先理解）**

```other
電腦（Windows / Mac）
        │
   區域網路（Wi-Fi / LAN）
        │
Raspberry Pi（Samba File Server）
        │
   /home/pi/share
```

---

## **三、步驟 1：安裝 Samba**

在 Raspberry Pi 上執行：

```other
sudo apt update
sudo apt install samba -y
```

確認安裝成功：

```other
smbd --version
```

---

## **四、步驟 2：建立分享資料夾**

```other
mkdir -p /home/pi/share
```

設定權限（教學用先開放）：

```other
chmod 777 /home/pi/share
```

> 教學階段這樣最單純

> 實務環境可再調整權限

---

## **五、步驟 3：設定 Samba 分享**

編輯設定檔：

```other
sudo nano /etc/samba/smb.conf
```

**在檔案最底下加入：**

```other
[share]
   path = /home/pi/share
   browseable = yes
   writable = yes
   guest ok = yes
   read only = no
```

👉 這表示：

- [share]：分享名稱
- guest ok = yes：不用帳密（教學友善）

儲存後重新啟動 Samba：

```other
sudo systemctl restart smbd
```

---

## **六、步驟 4：從電腦端連線**

### **🔹 Windows**

1. 檔案總管 → 位址列輸入：

```other
\\raspberrypi
```

1. 或

```other
\\RaspberryPi_IP
```

1. 點進 share 資料夾

---

### **🔹 macOS**

Finder → 前往 → 連接伺服器：

```other
smb://raspberrypi/share
```

或

```other
smb://192.168.1.50/share
```

---

### **🔹 Linux**

```other
smbclient //raspberrypi/share
```

或使用檔案管理員連線

---

## **七、進階（建議實務用）：加上帳號密碼**

### **1️⃣ 建立 Linux 使用者（如果還沒有）**

```other
sudo adduser student
```

### **2️⃣ 加入 Samba 使用者**

```other
sudo smbpasswd -a student
```

### **3️⃣ 修改 smb.conf**

```other
guest ok = no
valid users = student
```

---

## **八、開機自動啟動（通常已設定）**

確認狀態：

```other
systemctl status smbd
```

若沒啟動：

```other
sudo systemctl enable smbd
```

---

## **九、教學常見問題整理**

### **Q1：為什麼找不到 Raspberry Pi？**

- 檢查 IP：

```other
ip a
```

- 用 IP 直接連線最穩

---

### **Q2：中文檔名會不會亂碼？**

👉 **不會，只要系統是 UTF-8**

（你前一題問的 locale 正是關鍵）

---

### **Q3：可以當「雲端硬碟」嗎？**

- 區網內：可以
- 外網：搭配
    - Tailscale
    - Cloudflare Tunnel
    - VPN

---

## **十、其他 File Server 方案（給你比較）**

| **方案**      | **適合**     | **特點**   |
| ----------- | ---------- | -------- |
| **Samba** ⭐ | 教學 / 混合系統  | 最直覺      |
| NFS         | Linux-only | 效能最好     |
| FTP / SFTP  | 外網         | 有帳密      |
| Nextcloud   | 私有雲        | 有 Web UI |
| Syncthing   | 同步         | 非即時存取    |

---
