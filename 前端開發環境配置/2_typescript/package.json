{
  "name": "2_typescript",
  "version": "1.0.0",
  "description": "學習用",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:web": "npm run build && echo '✅ 網頁應用編譯完成！可以使用瀏覽器開啟 public/index.html'",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "clean": "rm -rf dist",
    "serve": "npx http-server public -p 8080 -o",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
