## Node.js_npm_環境配置

### 概述

在現代前端開發中，特別是使用 TypeScript 和 React 的專案，Node.js、npm 和 package.json 是開發環境的核心基礎設施。本文件將詳細介紹這三個重要概念，並說明它們在 TypeScript + React 專案開發和佈署中的作用。

---

## 📦 Node.js 是什麼？

### 定義

**Node.js** 是一個開源的、跨平台的 JavaScript 執行環境，基於 Chrome 的 V8 JavaScript 引擎。它讓開發者可以在伺服器端執行 JavaScript，也成為現代前端開發工具的運行基礎。

### 在前端開發中的重要性

雖然 Node.js 最初是為伺服器端開發設計，但在前端開發中扮演關鍵角色：

1. **開發工具運行環境**
   - TypeScript 編譯器（tsc）需要在 Node.js 環境中運行
   - React 開發伺服器（如 Vite、Webpack）需要 Node.js
   - 各種建置工具和打包工具都依賴 Node.js

2. **套件管理**
   - npm（Node Package Manager）是 Node.js 內建的套件管理器
   - 所有前端依賴套件都通過 Node.js 環境安裝和管理

3. **開發伺服器**
   - 本地開發時運行開發伺服器（Development Server）
   - 提供熱重載（Hot Reload）、代理等功能

### 安裝 Node.js

```bash
# 檢查 Node.js 版本
node --version

# 檢查 npm 版本
npm --version
```

**建議**：使用 LTS（Long Term Support）版本，確保穩定性和長期支援。

---

## 📋 npm 是什麼？

### 定義

**npm**（Node Package Manager）是 Node.js 的套件管理器，也是世界上最大的軟體註冊表。它讓開發者可以輕鬆地安裝、管理和分享 JavaScript 套件。

### 核心功能

1. **套件安裝**
   ```bash
   # 安裝套件到專案（會加入到 dependencies）
   npm install <package-name>
   
   # 安裝開發時依賴（會加入到 devDependencies）
   npm install --save-dev <package-name>
   
   # 全域安裝
   npm install -g <package-name>
   ```

### 三種安裝方式的詳細說明

這三種安裝方式，主要差在「裝到哪裡」以及「在專案生命週期扮演什麼角色」。

#### 1. `npm install <package-name>`

- **安裝位置**：裝到「目前專案」底下（`node_modules` 目錄）
- **寫入位置**：會寫進 `package.json` 的 **dependencies**
- **代表意義**：這個套件在「專案執行時」會用到
- **典型範例**：React、react-dom、axios、react-router-dom 等運行時需要的庫

```bash
npm install react react-dom
# 執行後會在 package.json 的 dependencies 中看到：
# "dependencies": {
#   "react": "^18.2.0",
#   "react-dom": "^18.2.0"
# }
```

#### 2. `npm install --save-dev <package-name>`

- **安裝位置**：一樣裝到目前專案（`node_modules` 目錄）
- **寫入位置**：會寫進 `package.json` 的 **devDependencies**
- **代表意義**：只在「開發或建置階段」會用到，不會被打包到生產版本
- **典型範例**：TypeScript、ESLint、Vite、測試框架（Vitest、Jest）、建置工具等

```bash
npm install --save-dev typescript vite @vitejs/plugin-react
# 執行後會在 package.json 的 devDependencies 中看到：
# "devDependencies": {
#   "typescript": "^5.2.0",
#   "vite": "^4.4.5",
#   "@vitejs/plugin-react": "^4.0.9"
# }
```

#### 3. `npm install -g <package-name>`

- **安裝位置**：裝到「全域環境」，不是專案目錄下
- **寫入位置**：**不會寫進**目前專案的 `package.json`
- **代表意義**：這個套件通常是當成「命令列工具」來用，可以在任何資料夾下執行指令
- **典型範例**：npm、pnpm、eslint CLI、typescript 的 tsc、create-vite、create-react-app 等全域工具

```bash
npm install -g typescript
# 安裝後可以在任何目錄下直接使用：
tsc --version
# 而不需要在專案中執行
```

### 什麼時候用哪一種？

- **專案執行會用到的庫** → 用 `npm install`，讓它進 `dependencies`
  - 例如：React、Vue、Angular、狀態管理庫、UI 元件庫等

- **只在開發、測試、建置用的工具** → 用 `npm install --save-dev`，讓它進 `devDependencies`
  - 例如：TypeScript、ESLint、Prettier、建置工具（Vite、Webpack）、測試框架等

- **想在任何專案、任何資料夾都可以直接下指令的工具** → 用 `npm install -g`
  - 例如：全域 CLI 工具、腳手架工具等

**建議**：在教學或實務專案中，通常會鼓勵先以「專案本地安裝」（前兩種）為主，只有真的需要全域 CLI 時才用 `-g`。這樣可以確保專案的依賴關係明確，並且不同專案可以使用不同版本的同一工具。

2. **專案初始化**
   ```bash
   # 創建新的 package.json 檔案
   npm init
   
   # 使用預設值快速初始化
   npm init -y
   ```

3. **腳本執行**
   ```bash
   # 執行 package.json 中定義的腳本
   npm run <script-name>
   
   # 執行測試
   npm test
   
   # 啟動專案
   npm start
   ```

### npm scripts 的詳細說明

可以把 `npm run`、`npm test`、`npm start` 全部當成「幫你下長指令的快捷鍵」。以下用具體情境來說明：

#### 核心概念

- **package.json = 遙控器的按鈕說明表**：  
  `scripts` 裡面每一行，就是幫某個「按鈕」取名字，例如：`"dev": "vite"`、`"build": "tsc && vite build"`。

- **npm run XXX = 按下那顆按鈕**：  
  `npm run dev` 就是「按下 dev 按鈕」，實際在背後幫你執行 `vite` 這個指令。

**重要觀念**：`npm run` 後面接的是「按鈕名字」，不是系統指令本身。

#### 實例 1：啟動開發伺服器（npm run dev）

假設 `package.json` 裡有這段：

```json
"scripts": {
  "dev": "vite"
}
```

**說明**：

- 如果不用 `npm run`，原本要打的指令是：`vite`
- 但使用 `npm run dev` 時，npm 會去 `package.json` 找 `dev` 這個腳本，幫你執行 `vite`
- **所以在開發時，只要記得打 `npm run dev` 就可以啟動開發伺服器**

**流程圖**：

```
學生打：npm run dev
  ↓
npm 去看 package.json
  ↓
找到 "dev": "vite"
  ↓
實際執行：vite
```

#### 實例 2：建置專案（npm run build）

`package.json` 範例：

```json
"scripts": {
  "build": "tsc && vite build"
}
```

**說明**：

- 真正的完整指令其實是：`tsc && vite build`，對初學者來說又長又難記
- 我們幫它取一個簡單的名字叫 `build`，之後只要打：`npm run build` 就代表「先編譯 TypeScript，再打包 React 專案」
- **要產生正式版時，只要記得打：`npm run build`**

**流程圖**：

```
學生打：npm run build
  ↓
npm 找 "build": "tsc && vite build"
  ↓
依序幫你跑 tsc 再跑 vite build
```

#### 實例 3：程式碼檢查、測試（npm run lint / npm test）

`package.json` 可能有：

```json
"scripts": {
  "lint": "eslint . --ext ts,tsx",
  "test": "vitest"
}
```

**說明**：

- 「如果你想檢查程式碼有沒有問題，就打：`npm run lint`。」背後實際在跑的是 `eslint . --ext ts,tsx`
- 「如果你要跑自動化測試，就打：`npm test`。」npm 會自動對應到 `"test": "vitest"` 這個腳本

**重點**：學生不需要背 `eslint . --ext ts,tsx` 或 `vitest`，只要記得那幾個常用按鈕名稱：`dev` / `build` / `lint` / `test`。

#### 為什麼有時候是 npm test / npm start，不用 run？

在 `npm` 裡有兩個「特例名稱」：

- `npm start` 會自動對應到 `"start": "..."` 這個腳本
- `npm test` 會自動對應到 `"test": "..."` 這個腳本

**說明**：

- 「`npm start` 就等於 `npm run start`，只是 npm 幫你省略了 `run`」
- 「`npm test` 也是一樣的道理」

**重要概念**：語法不同，但概念完全一樣，都是在執行 package.json 裡面對應的腳本。

### 在 TypeScript + React 專案中的常見用法

```bash
# 安裝 React 和相關套件
npm install react react-dom

# 安裝 TypeScript 和類型定義（開發依賴）
npm install --save-dev typescript @types/react @types/react-dom

# 安裝建置工具（如 Vite）
npm install --save-dev vite @vitejs/plugin-react

# 安裝測試工具
npm install --save-dev vitest @testing-library/react
```

---

## 📄 package.json 是什麼？

### 定義

**package.json** 是 Node.js 專案的核心配置文件，定義了專案的元數據、依賴套件、腳本指令等資訊。它是專案的「身份證」和「說明書」。

### 基本結構

```json
{
  "name": "my-react-app",
  "version": "1.0.0",
  "description": "我的 React + TypeScript 專案",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^4.3.0"
  }
}
```

### 重要欄位說明

#### 1. **基本資訊**
- `name`: 專案名稱（必須唯一，如果要發佈到 npm）
- `version`: 專案版本號（遵循語義化版本 SemVer）
- `description`: 專案描述
- `license`: 授權方式（如 MIT、ISC）
- `author`: 作者資訊

#### 2. **scripts（腳本指令）**

定義可在 `npm run` 命令中執行的腳本：

```json
"scripts": {
  "dev": "vite",                          // 開發伺服器
  "build": "tsc && vite build",           // 編譯 TypeScript 並建置生產版本
  "build:prod": "npm run build",          // 生產環境建置
  "preview": "vite preview",              // 預覽建置結果
  "lint": "eslint . --ext ts,tsx",        // 程式碼檢查
  "type-check": "tsc --noEmit",           // TypeScript 類型檢查
  "test": "vitest",                       // 執行測試
  "clean": "rm -rf dist node_modules"     // 清理建置檔案
}
```

**使用方式**：
```bash
npm run dev        # 啟動開發伺服器
npm run build      # 建置生產版本
npm run lint       # 執行程式碼檢查
```

#### 3. **dependencies（生產依賴）**

專案運行時需要的套件，會被打包到最終產品中：

```json
"dependencies": {
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "react-router-dom": "^6.15.0"
}
```

#### 4. **devDependencies（開發依賴）**

只在開發時需要的套件，不會被打包到生產版本：

```json
"devDependencies": {
  "typescript": "^5.2.0",
  "@types/react": "^18.2.0",
  "@types/react-dom": "^18.2.0",
  "@vitejs/plugin-react": "^4.0.9",
  "vite": "^4.4.5",
  "eslint": "^8.47.0",
  "@typescript-eslint/parser": "^6.4.0"
}
```

**為什麼要分開？**
- 減少生產版本的檔案大小
- 明確區分開發工具和運行時依賴
- 提高安裝速度（在生產環境只需安裝 dependencies）

#### 5. **其他重要欄位**

```json
{
  "type": "module",                    // 使用 ES 模組（import/export）
  "main": "dist/index.js",             // 主要進入點
  "module": "dist/index.esm.js",       // ES 模組版本
  "types": "dist/index.d.ts",          // TypeScript 類型定義檔案
  "engines": {                         // Node.js 版本要求
    "node": ">=18.0.0"
  },
  "browserslist": [                    // 瀏覽器支援範圍
    "> 1%",
    "last 2 versions"
  ]
}
```

---

## 🔄 三者之間的關係

```
┌─────────────────┐
│   Node.js       │  ← 執行環境（運行 JavaScript）
│   (執行環境)     │
└────────┬────────┘
         │
         │ 內建
         │
┌────────▼────────┐
│   npm           │  ← 套件管理器（安裝、管理套件）
│   (工具)        │
└────────┬────────┘
         │
         │ 讀取/寫入
         │
┌────────▼────────┐
│ package.json    │  ← 配置文件（定義專案資訊）
│   (配置)        │
└─────────────────┘
```

### 工作流程

1. **初始化專案**
   ```bash
   npm init -y              # 創建 package.json
   ```

2. **安裝依賴**
   ```bash
   npm install react        # npm 讀取 package.json，安裝套件到 node_modules
   ```

3. **執行腳本**
   ```bash
   npm run build            # npm 讀取 package.json 的 scripts，執行建置指令
   ```

---

## 🚀 在 TypeScript + React 專案中的實際應用

### 典型的工作流程

#### 1. **專案初始化**

```bash
# 創建新專案
npm create vite@latest my-react-app -- --template react-ts

# 進入專案目錄
cd my-react-app

# 安裝依賴
npm install
```

#### 2. **開發階段**

```json
"scripts": {
  "dev": "vite",                    // 啟動開發伺服器（支援熱重載）
  "type-check": "tsc --noEmit"      // 檢查 TypeScript 類型錯誤
}
```

```bash
npm run dev              # 啟動開發伺服器（通常在 http://localhost:5173）
```

#### 3. **建置階段**

```json
"scripts": {
  "build": "tsc && vite build"      // 編譯 TypeScript + 打包 React 應用
}
```

```bash
npm run build            # 生成生產版本的檔案到 dist/ 目錄
```

#### 4. **部署階段**

```bash
# 生產環境安裝（只安裝 dependencies，不安裝 devDependencies）
npm install --production

# 建置專案
npm run build

# 部署 dist/ 目錄的內容到靜態網站托管服務
# （如 Vercel、Netlify、GitHub Pages 等）
```

---

## 📦 package-lock.json 和 node_modules

### package-lock.json

- **作用**：鎖定所有依賴套件的確切版本
- **重要性**：確保不同環境下安裝相同版本的套件
- **建議**：應該提交到版本控制系統（Git）

### node_modules

- **作用**：存放所有安裝的套件和其依賴
- **特點**：檔案很大，通常不應該提交到 Git
- **管理**：通過 `.gitignore` 排除，透過 `npm install` 重新生成

---

## 🎯 最佳實踐

1. **版本管理**
   - 使用語義化版本（SemVer）
   - 使用 `^` 或 `~` 控制版本更新範圍

2. **依賴分類**
   - 明確區分 `dependencies` 和 `devDependencies`
   - 生產環境只需 `dependencies`

3. **腳本組織**
   - 使用描述性的腳本名稱
   - 組合多個指令時使用 `&&`

4. **環境變數**
   - 使用 `.env` 檔案管理環境變數
   - 不同環境使用不同的配置檔案

5. **安全性**
   - 定期更新依賴套件
   - 使用 `npm audit` 檢查安全性漏洞

---

## 🔗 相關資源

- [Node.js 官方文件](https://nodejs.org/zh-tw/docs)
- [npm 官方文件](https://docs.npmjs.com/)
- [package.json 完整文件](https://docs.npmjs.com/cli/v9/configuring-npm/package-json)
- [語義化版本規範](https://semver.org/lang/zh-TW/)

---

## 📝 總結

- **Node.js**：提供 JavaScript 執行環境，讓前端開發工具得以運行
- **npm**：管理專案的套件依賴，是安裝和管理工具的介面
- **package.json**：定義專案配置，是專案的「說明書」和「指令集」

三者協同工作，構成了現代前端開發（特別是 TypeScript + React）的基礎架構，讓開發者能夠高效地開發、建置和部署應用程式。
