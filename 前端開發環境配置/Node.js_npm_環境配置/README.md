# Node.js 與 npm 環境配置

> **快速理解 Node.js、npm 和 package.json 在前端開發中的角色**

---

## 🎯 快速理解（30 秒版本）

### 三個東西的關係

```
Node.js = 執行環境（讓工具可以跑）
    ↓
npm = 套件管理器（幫你安裝工具）
    ↓
package.json = 專案說明書（記錄專案資訊和工具清單）
```

### 生活化比喻

- **Node.js** = 電源插座（提供電力讓電器運作）
- **npm** = 購物網站（幫你下載和安裝工具）
- **package.json** = 購物清單（記錄你買了什麼工具）

---

## 📦 Node.js 是什麼？

### 一句話說明

> **Node.js 是讓 JavaScript 可以在電腦上執行的環境**

### 為什麼前端開發需要 Node.js？

雖然前端程式碼最終在瀏覽器執行，但**開發工具**需要在 Node.js 環境中運行：

| 工具 | 為什麼需要 Node.js |
|------|------------------|
| TypeScript 編譯器 | 需要在電腦上執行，把 TypeScript 轉成 JavaScript |
| Vite / Webpack | 開發伺服器和打包工具需要在電腦上執行 |
| ESLint | 程式碼檢查工具需要在電腦上執行 |
| npm | 套件管理器本身需要 Node.js |

### 安裝與檢查

```bash
# 檢查是否已安裝
node --version
npm --version

# 如果沒有安裝，請到官網下載：
# https://nodejs.org/
# 建議選擇 LTS（長期支援）版本
```

**重要**：安裝 Node.js 後，npm 會自動一起安裝（npm 是 Node.js 內建的）。

---

## 📋 npm 是什麼？

### 一句話說明

> **npm 是套件管理器，幫你下載和管理 JavaScript 工具和函式庫**

### npm 的三種安裝方式

#### 1. 專案依賴（最常用）

```bash
npm install react
```

**說明**：
- 安裝到專案的 `node_modules/` 資料夾
- 會寫入 `package.json` 的 `dependencies`
- **用途**：專案執行時需要的套件（例如 React、Vue）

**範例**：
```bash
npm install react react-dom
# 執行後，package.json 會出現：
# "dependencies": {
#   "react": "^18.2.0",
#   "react-dom": "^18.2.0"
# }
```

#### 2. 開發依賴（開發工具）

```bash
npm install --save-dev typescript
# 或簡寫
npm install -D typescript
```

**說明**：
- 安裝到專案的 `node_modules/` 資料夾
- 會寫入 `package.json` 的 `devDependencies`
- **用途**：只在開發時需要的工具（例如 TypeScript、Vite、測試工具）

**範例**：
```bash
npm install -D typescript vite
# 執行後，package.json 會出現：
# "devDependencies": {
#   "typescript": "^5.0.0",
#   "vite": "^5.0.0"
# }
```

#### 3. 全域安裝（命令列工具）

```bash
npm install -g typescript
```

**說明**：
- 安裝到系統全域位置（不是專案資料夾）
- **不會**寫入 `package.json`
- **用途**：命令列工具，可以在任何地方使用

**範例**：
```bash
npm install -g typescript
# 安裝後，可以在任何地方使用：
tsc --version
```

### 快速判斷：該用哪一種？

| 情況 | 使用方式 | 範例 |
|------|---------|------|
| 專案執行時需要 | `npm install` | React、Vue、axios |
| 開發工具 | `npm install -D` | TypeScript、Vite、ESLint |
| 命令列工具 | `npm install -g` | create-react-app、tsc（全域） |

**建議**：初學者優先使用前兩種（專案本地安裝），只有真的需要全域工具時才用 `-g`。

---

## 📄 package.json 是什麼？

### 一句話說明

> **package.json 是專案的「身份證」和「說明書」，記錄專案資訊、依賴套件和執行腳本**

### 基本結構

```json
{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

### 重要欄位說明

#### 1. 基本資訊

```json
{
  "name": "my-project",        // 專案名稱
  "version": "1.0.0",          // 版本號
  "description": "我的專案"    // 專案描述
}
```

#### 2. scripts（執行腳本）⭐ 最重要

**概念**：幫常用指令取「快捷鍵」

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  }
}
```

**使用方式**：
```bash
npm run dev      # 執行 "dev" 腳本，實際跑的是 "vite"
npm run build    # 執行 "build" 腳本，實際跑的是 "tsc && vite build"
npm run preview  # 執行 "preview" 腳本，實際跑的是 "vite preview"
```

**為什麼需要 scripts？**

- 原本要打：`vite`（簡單）
- 原本要打：`tsc && vite build`（複雜又難記）
- 現在只要打：`npm run dev`、`npm run build`（好記又統一）

**常用 scripts 說明**：

| 腳本名稱 | 實際指令 | 用途 |
|---------|---------|------|
| `dev` | `vite` | 啟動開發伺服器（通常 http://localhost:5173） |
| `build` | `tsc && vite build` | 編譯 TypeScript 並打包專案 |
| `preview` | `vite preview` | 預覽打包後的結果 |
| `lint` | `eslint .` | 檢查程式碼品質 |
| `test` | `vitest` | 執行測試 |

**特殊語法**：
- `npm start` = `npm run start`（npm 自動省略 `run`）
- `npm test` = `npm run test`（npm 自動省略 `run`）

#### 3. dependencies（生產依賴）

**專案執行時需要的套件**，會被打包到最終產品中：

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
```

**範例套件**：React、Vue、axios、lodash 等

#### 4. devDependencies（開發依賴）

**只在開發時需要的工具**，不會被打包到生產版本：

```json
{
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "eslint": "^8.0.0"
  }
}
```

**範例套件**：TypeScript、Vite、Webpack、測試工具等

**為什麼要分開？**

- 生產環境不需要開發工具（減少檔案大小）
- 部署時只安裝 `dependencies`（加快安裝速度）
- 清楚區分「執行時需要」和「開發時需要」

---

## 🔄 三者之間的關係

### 關係圖

```
┌─────────────┐
│  Node.js    │  ← 執行環境（讓工具可以跑）
│  (執行環境)  │
└──────┬──────┘
       │
       │ 內建
       │
┌──────▼──────┐
│     npm     │  ← 套件管理器（安裝工具）
│   (工具)    │
└──────┬──────┘
       │
       │ 讀取/寫入
       │
┌──────▼──────┐
│ package.json│  ← 配置文件（記錄專案資訊）
│   (配置)    │
└─────────────┘
```

### 實際工作流程

#### 1. 初始化專案

```bash
npm init -y
# 建立 package.json 檔案
```

#### 2. 安裝套件

```bash
npm install react
# npm 讀取 package.json
# 下載 react 到 node_modules/
# 更新 package.json 的 dependencies
```

#### 3. 執行腳本

```bash
npm run dev
# npm 讀取 package.json 的 scripts
# 找到 "dev": "vite"
# 執行 vite 指令
```

---

## 🚀 實際應用範例

### 建立一個 React + TypeScript 專案

#### 步驟 1：初始化專案

```bash
# 建立專案資料夾
mkdir my-app
cd my-app

# 初始化 package.json
npm init -y
```

#### 步驟 2：安裝依賴

```bash
# 安裝 React（執行時需要）
npm install react react-dom

# 安裝開發工具
npm install -D typescript @types/react @types/react-dom vite @vitejs/plugin-react
```

#### 步驟 3：設定 scripts

編輯 `package.json`：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  }
}
```

#### 步驟 4：開始開發

```bash
npm run dev
# 啟動開發伺服器，開始寫程式碼
```

---

## 📦 重要檔案說明

### package-lock.json

**作用**：鎖定所有套件的確切版本

**為什麼重要**：
- 確保不同環境安裝相同版本
- 避免「在我電腦上可以跑，在你電腦上不行」的問題

**建議**：應該提交到 Git（版本控制）

### node_modules/

**作用**：存放所有安裝的套件

**特點**：
- 檔案很大（可能幾百 MB 到幾 GB）
- 可以重新產生（只要有 `package.json`）

**建議**：
- **不要**提交到 Git
- 在 `.gitignore` 中加入 `node_modules/`
- 需要時執行 `npm install` 重新產生

### .gitignore 範例

```
node_modules/
dist/
*.log
.DS_Store
```

---

## 🎯 常用指令速查表

### 專案初始化

```bash
npm init -y              # 建立 package.json（使用預設值）
```

### 安裝套件

```bash
npm install <套件名>      # 安裝到 dependencies
npm install -D <套件名>   # 安裝到 devDependencies
npm install -g <套件名>   # 全域安裝
npm install              # 安裝 package.json 中所有依賴
```

### 執行腳本

```bash
npm run <腳本名稱>        # 執行自訂腳本
npm start                # 執行 "start" 腳本
npm test                 # 執行 "test" 腳本
```

### 其他常用指令

```bash
npm uninstall <套件名>   # 移除套件
npm update               # 更新套件
npm list                 # 查看已安裝的套件
npm audit                # 檢查安全性漏洞
```

---

## ⚠️ 常見問題與解決方法

### Q1: 執行 `npm install` 時出現錯誤

**可能原因**：
- Node.js 版本太舊
- 網路連線問題
- 權限問題

**解決方法**：
```bash
# 檢查 Node.js 版本（建議 18+）
node --version

# 清除 npm 快取
npm cache clean --force

# 刪除 node_modules 和 package-lock.json 後重新安裝
rm -rf node_modules package-lock.json
npm install
```

### Q2: `npm run dev` 找不到指令

**可能原因**：
- 沒有安裝依賴
- package.json 中沒有定義該腳本

**解決方法**：
```bash
# 先安裝依賴
npm install

# 檢查 package.json 是否有定義 scripts
cat package.json
```

### Q3: 套件版本衝突

**可能原因**：
- 不同套件需要不同版本的依賴

**解決方法**：
```bash
# 刪除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安裝
npm install
```

### Q4: 安裝速度很慢

**解決方法**：
```bash
# 使用國內鏡像（例如淘寶鏡像）
npm config set registry https://registry.npmmirror.com

# 或使用 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

---

## 💡 最佳實踐

### 1. 版本管理

- 使用語義化版本（SemVer）
- `^` 允許小版本更新：`^18.2.0` 可以安裝 `18.2.1`、`18.3.0`，但不能安裝 `19.0.0`
- `~` 只允許修補版本更新：`~18.2.0` 可以安裝 `18.2.1`，但不能安裝 `18.3.0`

### 2. 依賴分類

- 明確區分 `dependencies` 和 `devDependencies`
- 生產環境只需要 `dependencies`

### 3. 腳本命名

- 使用描述性的名稱：`dev`、`build`、`test`
- 組合指令使用 `&&`：`"build": "tsc && vite build"`

### 4. 安全性

- 定期更新套件：`npm update`
- 檢查安全性：`npm audit`
- 修復漏洞：`npm audit fix`

---

## 📚 延伸學習

### 官方資源

- [Node.js 官方文件](https://nodejs.org/zh-tw/docs)
- [npm 官方文件](https://docs.npmjs.com/)
- [package.json 完整文件](https://docs.npmjs.com/cli/v9/configuring-npm/package-json)

### 相關教學

- [語義化版本規範](https://semver.org/lang/zh-TW/)
- [npm scripts 進階用法](https://docs.npmjs.com/cli/v9/using-npm/scripts)

---

## 🎓 總結

### 三個核心概念

1. **Node.js** = 執行環境
   - 讓開發工具可以在電腦上運行
   - 前端開發工具都需要它

2. **npm** = 套件管理器
   - 幫你下載和管理工具
   - 三種安裝方式：專案依賴、開發依賴、全域安裝

3. **package.json** = 專案說明書
   - 記錄專案資訊和依賴
   - 定義執行腳本（快捷鍵）
   - 區分生產依賴和開發依賴

### 記住這些就夠了

- `npm install` = 安裝套件
- `npm run dev` = 啟動開發伺服器
- `npm run build` = 打包專案
- `dependencies` = 執行時需要
- `devDependencies` = 開發時需要

---

**記住：理解這三個概念，前端開發環境就掌握一半了！** 🚀
