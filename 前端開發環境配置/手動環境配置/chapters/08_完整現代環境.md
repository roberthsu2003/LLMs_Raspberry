# 第 8 章：React + TypeScript + Vite（完整現代環境）

## 🎯 教學目標

完成本章後，你應該能夠：

- 能清楚說出每一層技術在做什麼
- 理解現代前端專案的標準組成
- 能判斷何時該用、何時不需要這麼完整的環境
- 理解各層技術之間的關係
- 能夠手動配置一個完整的現代前端專案

---

## 📖 核心觀念

### 整合所有概念

到目前為止，我們學到了：

1. **語言層**：TypeScript
2. **編譯層**：TypeScript Compiler
3. **模組系統**：ES Module
4. **開發工具**：Vite
5. **框架層**：React

現在，我們把它們全部整合起來！

---

## 🏗️ 完整專案結構

### 範例 F：React + TypeScript + Vite（完整現代環境）

> 💡 **實際範例**：完整的範例專案請參考 [範例 F：完整現代環境](../examples/F_react-ts-vite/README.md)

**📂 完整範例專案：** [examples/F_react-ts-vite](../examples/F_react-ts-vite/)

```
react-ts-vite/
├── index.html              # 入口 HTML
├── src/
│   ├── App.tsx            # 主應用元件
│   ├── main.tsx           # 入口檔案
│   └── components/        # 元件資料夾
│       └── Button.tsx
├── public/                 # 靜態資源
├── vite.config.ts         # Vite 配置
├── tsconfig.json          # TypeScript 配置
└── package.json
```

---

## 📝 完整檔案內容

### `index.html`

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>React + TypeScript + Vite</title>
</head>
<body>
    <div id="root"></div>
    <!-- 注意：直接引用 TypeScript 檔案 -->
    <script type="module" src="/src/main.tsx"></script>
</body>
</html>
```

### `src/main.tsx`

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// 型別安全的根元素
const rootElement = document.getElementById('root');

if (!rootElement) {
    throw new Error('Root element not found');
}

const root = ReactDOM.createRoot(rootElement);
root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
```

### `src/App.tsx`

```tsx
import { useState } from 'react';
import Button from './components/Button';

function App() {
    const [count, setCount] = useState<number>(0);

    return (
        <div>
            <h1>React + TypeScript + Vite</h1>
            <p>Count: {count}</p>
            <Button 
                text="增加" 
                onClick={() => setCount(count + 1)} 
            />
            <Button 
                text="減少" 
                onClick={() => setCount(count - 1)} 
            />
        </div>
    );
}

export default App;
```

### `src/components/Button.tsx`

```tsx
interface ButtonProps {
    text: string;
    onClick: () => void;
}

function Button({ text, onClick }: ButtonProps) {
    return (
        <button onClick={onClick}>
            {text}
        </button>
    );
}

export default Button;
```

### `vite.config.ts`

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
    plugins: [react()],
    // 可以添加更多配置
    server: {
        port: 3000,
        open: true,
    },
    build: {
        outDir: 'dist',
    },
});
```

### `tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### `package.json`

```json
{
  "name": "react-ts-vite",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vitejs/plugin-react": "^4.2.0",
    "eslint": "^8.45.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.3",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

---

## 🔄 各層技術的關係

### 技術棧圖

```
┌─────────────────────────────────────┐
│   React（框架層）                    │
│   - 解決 UI 問題                     │
│   - Component 思維                  │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│   TypeScript（語言層）               │
│   - 型別安全                         │
│   - 開發時檢查                       │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│   Vite（工具層）                      │
│   - 開發伺服器                       │
│   - 打包工具                         │
│   - 整合所有工具                     │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│   JavaScript（執行層）                │
│   - 瀏覽器執行                       │
│   - 最終產物                         │
└─────────────────────────────────────┘
```

### 各層的責任

#### 1. React（框架層）

**責任**：
- 管理 UI 狀態
- 處理使用者互動
- 元件化開發

**輸入**：JSX + Props + State  
**輸出**：Virtual DOM

#### 2. TypeScript（語言層）

**責任**：
- 型別檢查
- 開發時錯誤提示
- 程式碼文件化

**輸入**：TypeScript 程式碼  
**輸出**：JavaScript（透過 tsc）

#### 3. Vite（工具層）

**責任**：
- 開發伺服器
- JSX 轉換
- TypeScript 編譯
- 打包和優化

**輸入**：原始碼（TSX、TS、JSX）  
**輸出**：可執行的 JavaScript

#### 4. JavaScript（執行層）

**責任**：
- 在瀏覽器中執行
- 操作 DOM
- 處理事件

**輸入**：編譯後的 JavaScript  
**輸出**：使用者看到的網頁

---

## 🎯 開發流程

### 開發階段

```bash
npm run dev
```

**流程**：
1. Vite 啟動開發伺服器
2. 讀取 `index.html`
3. 遇到 `<script src="/src/main.tsx">`
4. Vite 處理 `main.tsx`：
   - TypeScript 編譯 → JavaScript
   - JSX 轉換 → React.createElement
   - 處理 import（React、其他元件）
5. 返回給瀏覽器執行
6. 修改檔案時自動重新編譯和更新

### 建置階段

```bash
npm run build
```

**流程**：
1. TypeScript 編譯檢查（`tsc`）
2. Vite 打包：
   - 編譯所有 TypeScript/JSX
   - 打包所有檔案
   - 優化程式碼（壓縮、Tree Shaking）
   - 處理資源（CSS、圖片）
3. 輸出到 `dist/` 資料夾
4. 準備部署

---

## 💡 每一層的價值

### React 的價值

- ✅ 元件化開發
- ✅ 狀態管理
- ✅ 虛擬 DOM（效能優化）

### TypeScript 的價值

- ✅ 型別安全
- ✅ 開發時錯誤檢查
- ✅ IDE 自動完成

### Vite 的價值

- ✅ 快速開發伺服器
- ✅ 熱更新
- ✅ 工具整合
- ✅ 快速建置

---

## 🎓 關鍵理解

### 每一層都有責任

> **每一層技術解決不同的問題，組合起來形成完整的開發環境**

- React：UI 問題
- TypeScript：型別問題
- Vite：開發流程問題

### 拿掉某一層會發生什麼？

#### 拿掉 React

- 需要手動操作 DOM
- 失去元件化優勢
- 失去狀態管理

#### 拿掉 TypeScript

- 失去型別安全
- 失去開發時錯誤檢查
- 失去 IDE 自動完成

#### 拿掉 Vite

- 需要手動配置開發伺服器
- 需要手動處理 JSX 轉換
- 需要手動處理打包

### 何時該用？何時不需要？

#### 應該使用完整環境的情況

- ✅ 大型專案
- ✅ 團隊協作
- ✅ 需要型別安全
- ✅ 需要良好的開發體驗

#### 不需要完整環境的情況

- ✅ 簡單的靜態網頁
- ✅ 學習目的（理解原理）
- ✅ 快速原型開發

---

## ⚠️ 學生常見迷思

### ❌ 迷思 1：「這是唯一正確的前端做法」

**事實**：這只是其中一種方式，還有其他選擇。

**為什麼**：不同的專案有不同的需求，選擇適合的工具即可。

### ❌ 迷思 2：「所有專案都一定要這套」

**事實**：簡單的專案可能不需要這麼完整的環境。

**為什麼**：工具是手段，不是目的，根據需求選擇。

### ❌ 迷思 3：「模板產生的東西不需要理解」

**事實**：理解每一層的作用很重要。

**為什麼**：遇到問題時需要知道如何除錯和調整。

### ❌ 迷思 4：「出錯只能砍掉重來」

**事實**：理解結構後可以逐步除錯和修復。

**為什麼**：每一層都有明確的責任，可以針對性解決問題。

---

## 🎓 本章重點回顧

1. **完整環境的組成**
   - React：UI 框架
   - TypeScript：型別系統
   - Vite：開發工具

2. **各層的責任**
   - 每一層解決不同的問題
   - 組合起來形成完整環境

3. **開發流程**
   - 開發階段：Vite Dev Server
   - 建置階段：Vite Build

4. **何時使用**
   - 根據專案需求選擇
   - 不是所有專案都需要

---

## 🎉 恭喜完成！

你已經完成了整個前端環境配置的學習！

現在你應該能夠：
- ✅ 理解前端環境的五個層次
- ✅ 知道每個工具解決了什麼問題
- ✅ 能夠手動配置一個前端專案
- ✅ 理解現代前端工具鏈的工作原理

---

## 💪 最終練習

1. **實作題**：從零開始建立一個完整的 React + TypeScript + Vite 專案

2. **對照題**：比較「沒有工具」、「部分工具」、「完整工具」的差異

3. **研究題**：探索其他工具（Webpack、Rollup、Parcel），了解它們的差異

4. **專案題**：用完整環境建立一個實際的專案（例如待辦事項、部落格）

---

## 📚 延伸學習

- [React 官方文件](https://react.dev/)
- [TypeScript 官方文件](https://www.typescriptlang.org/)
- [Vite 官方文件](https://vitejs.dev/)
- [React TypeScript Cheatsheet](https://react-typescript-cheatsheet.netlify.app/)

---

**祝學習愉快！Happy Coding! 🚀**
