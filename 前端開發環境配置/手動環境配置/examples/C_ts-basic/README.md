# 範例 C：TypeScript 基礎（無框架、無 Vite）

## 教學重點

這個範例展示：
- TypeScript 編譯流程
- tsc 的作用
- TypeScript → JavaScript 的轉換
- TypeScript 永遠不會在瀏覽器執行

## 檔案結構

```
C_ts-basic/
├── src/
│   └── main.ts          # TypeScript 原始碼
├── dist/                # 編譯輸出（自動產生）
│   └── main.js          # 編譯後的 JavaScript
├── index.html           # HTML 檔案（引用 dist/main.js）
├── tsconfig.json        # TypeScript 配置
├── package.json
└── README.md
```

## 如何執行

### 步驟 1：安裝依賴

```bash
npm install
```

### 步驟 2：編譯 TypeScript

```bash
npm run build
```

這會執行 `tsc`（TypeScript Compiler），把 `src/main.ts` 編譯成 `dist/main.js`。

### 步驟 3：查看編譯結果

打開 `dist/main.js`，你會看到：

```javascript
// 注意：型別註解都被移除了
function add(a, b) {
    return a + b;
}
```

**觀察**：
- 型別註解（`: number`、`: string`）被移除了
- 其他程式碼保持不變
- 這就是「轉譯」的過程

### 步驟 4：在瀏覽器中開啟

**方法 1：直接開啟**
- 雙擊 `index.html`
- 注意：HTML 引用的是 `dist/main.js`，不是 `src/main.ts`

**方法 2：使用本地伺服器**
```bash
npm run serve
```

## 關鍵點

### 1. TypeScript 需要編譯

```typescript
// src/main.ts（TypeScript）
function add(a: number, b: number): number {
    return a + b;
}
```

↓ 編譯（tsc）↓

```javascript
// dist/main.js（JavaScript）
function add(a, b) {
    return a + b;
}
```

### 2. 瀏覽器執行的是 JavaScript

- 瀏覽器執行 `dist/main.js`
- 瀏覽器**不執行** `src/main.ts`
- TypeScript 永遠不會在瀏覽器執行

### 3. 開發流程

```
寫 TypeScript → 編譯（tsc）→ JavaScript → 瀏覽器執行
```

每次修改 TypeScript 後，都需要重新編譯。

## 教學流程建議

1. **寫 TypeScript**：在 `src/main.ts` 中寫程式碼
2. **編譯**：執行 `npm run build`
3. **查看結果**：打開 `dist/main.js`，看看編譯後的程式碼
4. **問學生**：「瀏覽器實際跑的是哪一個？」

## 觀察重點

1. **型別被移除**：編譯後的 JavaScript 沒有型別
2. **語法轉換**：TypeScript 語法轉換成 JavaScript
3. **開發時檢查**：TypeScript 的錯誤只在開發時出現

## 學習目標

完成這個範例後，你應該能夠：
- ✅ 理解 TypeScript 編譯流程
- ✅ 知道 TypeScript 永遠不會在瀏覽器執行
- ✅ 理解 tsc 的作用

## 延伸練習

1. 嘗試寫一些型別錯誤，看看 TypeScript 如何提示
2. 修改 `tsconfig.json`，觀察編譯結果的變化
3. 使用 `npm run watch`，體驗自動編譯

## 對應章節

👉 [第 3 章：TypeScript 的本質](../../chapters/03_TypeScript的本質.md)
